# Runtime key scoped to the dedicated Secure MCP Tunnel; do not use an admin key in the daemon.
CONTROL_PLANE_API_KEY=

# Full downstream HTTP Authorization value. Example shape only:
# JOB_HARNESS_TUNNEL_AUTH_HEADER=Bearer <JOB_HARNESS_AUTH_TOKEN>
JOB_HARNESS_TUNNEL_AUTH_HEADER=
