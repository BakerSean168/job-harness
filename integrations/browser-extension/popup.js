const state = document.getElementById("state"); const meta = document.getElementById("meta"); document.getElementById("options").addEventListener("click", () => chrome.runtime.openOptionsPage()); chrome.runtime.sendMessage({ type: "JH_BRIDGE_STATUS" }, (response) => { if (chrome.runtime.lastError || !response) { state.textContent = "未连接"; return; } state.textContent = response.state.connected ? "已连接" : "未连接"; meta.textContent = `${response.settings.agentId || "—"} · ${response.state.detail || ""}`; });
